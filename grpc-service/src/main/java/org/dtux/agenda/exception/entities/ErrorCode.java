package org.dtux.agenda.exception.entities;

public enum ErrorCode {
    INVALID_PARAMETER,
    BUSINESS_ERROR,
    GATEWAY_ERROR,
    REPOSITORY_ERROR,
    NOT_FOUND,
    INTERNAL_ERROR,
    FORBIDDEN
}
